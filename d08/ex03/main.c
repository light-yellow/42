#include "ft_abs.h"
#include <stdio.h>

int main(void)
{
    printf("absolute value of 42 is %d\n", ABS(42));
    printf("absolute value of -42 is %d\n", ABS(-42));
    printf("absolute value of 0 is %d\n", ABS(0));
    return (0);
}
